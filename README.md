# Telegram Math Bot 📐📈

Бот для визуализации алгебраических формул и геометрических фигур.
Написан на Python с использованием `python-telegram-bot` и `matplotlib`.

## ✨ Возможности

- 📈 **Построение графиков функций**:
  - Поддержка `sin`, `cos`, `tan`, `log`, `sqrt`, `exp`, `abs`
  - Степени `^`, `**`
  - Несколько графиков на одном изображении (через `;`)
- 📐 **Геометрические фигуры**:
  - Круг, Треугольник, Прямоугольник, Эллипс
- 🇷🇺 **Полная поддержка русского языка**
- 🖼 **Генерация изображений высокого качества**

## 🚀 Установка и запуск

### Предварительные требования
- Python 3.10+
- Токен бота от [@BotFather](https://t.me/BotFather)

### Пошаговая инструкция

1. **Клонируйте репозиторий** (или скачайте файлы):
   ```bash
   git clone https://github.com/zomurb/telegram_math_bot.git
   cd telegram_math_bot
   ```

2. **Создайте виртуальное окружение**:
   ```bash
   python -m venv venv
   # Windows
   .\venv\Scripts\activate
   # Linux/MacOS
   source venv/bin/activate
   ```

3. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте переменные окружения**:
   - Откройте файл `.env`.
   - Вставьте ваш токен: `BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`

5. **Запустите бота**:
   ```bash
   python bot.py
   ```

## 📖 Использование

### Команды
- `/start` - Запустить бота
- `/help` - Справка
- `/examples` - Примеры

### Примеры формул
Просто отправьте боту сообщение:

**Алгебра:**
- `y = x^2`
- `y = sin(x) + cos(0.5*x)`
- `y = x^2; y = x^3` (два графика)

**Геометрия:**
- `круг r=5`
- `треугольник a=3 b=4 c=5`
- `прямоугольник a=10 b=5`
- `эллипс a=5 b=3`

## 🛠 Структура проекта
- `bot.py`: Основной файл запуска и обработка Telegram событий.
- `parser.py`: Разбор текстовых команд.
- `visualizer.py`: Отрисовка графиков через Matplotlib.
- `geometry.py`: Классы геометрических фигур.
- `config.py`: Настройки.
